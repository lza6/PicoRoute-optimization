# Database Structure

> [!NOTE]
> This file tracks the database schema for the PicoRoute project.
> Currently, the project is file-based, but v3.0 will introduce a DB Abstraction Layer.

## Planned Schemas (v3.0)

*(Prior to DB implementation, this section is invalid)*
